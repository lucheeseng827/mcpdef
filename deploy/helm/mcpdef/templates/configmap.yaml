apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcpdef.fullname" . }}
  labels:
    {{- include "mcpdef.labels" . | nindent 4 }}
data:
  mcpdef.toml: |
    {{- .Values.config | nindent 4 }}
